# 🏥 中醫舌診 AI 智能助手

**基於深度學習與 LangGraph Agent 的舌診分析系統**

---

## 📋 目錄

1. [專案概述](#專案概述)
2. [系統架構](#系統架構)
3. [核心功能](#核心功能)
4. [技術棧](#技術棧)
5. [長期記憶機制](#長期記憶機制)
6. [雷達圖評分系統](#雷達圖評分系統)
7. [使用流程](#使用流程)
8. [特色亮點](#特色亮點)
9. [未來發展](#未來發展)

---

## 🎯 專案概述

### 什麼是舌診 AI 助手？

一個結合**深度學習影像辨識**與**大型語言模型（LLM）**的智能中醫舌診分析系統，能夠：

- 📸 **自動分析舌頭圖片**：識別舌色、舌苔、裂紋、齒痕等 8 種中醫特徵
- 🤖 **AI 智能對話**：基於 LangGraph Agent 提供專業中醫建議
- 📊 **健康趨勢追蹤**：長期記憶機制記錄每次診斷，生成健康雷達圖
- 💬 **自然語言交互**：支持流式對話，即時回饋分析結果

### 解決的問題

| 傳統中醫舌診 | 本系統優勢 |
|------------|----------|
| ❌ 需要專業醫師判讀 | ✅ AI 自動分析，即時回饋 |
| ❌ 難以追蹤長期變化 | ✅ 自動記錄，生成趨勢圖表 |
| ❌ 主觀判斷，缺乏量化 | ✅ 標準化評分（0-10分） |
| ❌ 無法隨時隨地使用 | ✅ 桌面應用，隨拍隨診 |

---

## 🏗️ 系統架構

### 整體架構圖

```
┌─────────────────────────────────────────────────────────────┐
│                    Electron 桌面應用                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  React UI (TypeScript + Tailwind CSS)               │   │
│  │  - 聊天介面                                           │   │
│  │  - 相機拍照                                           │   │
│  │  - 健康週報雷達圖                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────────┘
                        │ HTTP API
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              FastAPI 後端服務 (Python)                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  LangGraph Agent (Ollama LLM: qwen3:8b)             │   │
│  │  - Chat Agent (一般對話)                             │   │
│  │  - Tongue Agent (舌診分析)                           │   │
│  │  - Tool Agent (可調用工具)                           │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  記憶管理模組                                          │   │
│  │  - 短期記憶：LangGraph MemorySaver (會話級別)         │   │
│  │  - 長期記憶：PostgreSQL (跨會話持久化)               │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  評分系統 (TongueHealthScorer)                       │   │
│  │  - 症狀權重映射                                        │   │
│  │  - 六維度體質評分                                      │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│              Vision-Predict 模組 (PyTorch)                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  舌頭分割模型：Swim Transformer (384x384)             │   │
│  │  - 自動提取舌頭 ROI                                    │   │
│  │  - 背景去除                                            │   │
│  └──────────────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  多標籤分類模型：ConvNeXt Base                        │   │
│  │  - 8 種舌診特徵識別                                    │   │
│  │  - 輸出概率與置信度                                    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 三層架構設計

1. **前端層（Electron + React）**
   - 跨平台桌面應用
   - 即時相機拍攝
   - 流式對話顯示
   - 雷達圖視覺化

2. **業務層（FastAPI + LangGraph）**
   - RESTful API 設計
   - Agent 智能決策
   - 記憶管理
   - 評分計算

3. **模型層（PyTorch）**
   - 舌頭分割
   - 特徵分類
   - 結果結構化輸出

---

## 🚀 核心功能

### 1. 智能舌診分析

#### 工作流程
```
用戶上傳照片
    ↓
舌頭分割（Swim Transformer）
    ↓
ROI 提取
    ↓
特徵分類（ConvNeXt）
    ↓
輸出 8 種舌診特徵
    ↓
LLM 生成中醫分析報告
    ↓
自動保存到長期記憶
```

#### 可識別的 8 種舌診特徵

| 英文標籤 | 中文名稱 | 中醫意義 |
|---------|---------|---------|
| TonguePale | 舌淡白 | 氣虛、血虛、陽虛 |
| TipSideRed | 舌尖邊紅 | 陰虛、濕熱 |
| Spot | 紅點 | 血瘀、濕熱 |
| Ecchymosis | 瘀斑 | 血瘀 |
| Crack | 裂紋 | 陰虛、氣虛 |
| Toothmark | 齒痕 | 氣虛、陽虛 |
| FurThick | 苔厚 | 濕熱、氣虛 |
| FurYellow | 苔黃 | 濕熱 |

### 2. 長期記憶機制

#### 記憶架構

**短期記憶（Session Memory）**
- 使用 LangGraph MemorySaver
- 保存會話級別的對話歷史
- 每次應用啟動生成新的 session_id
- 支持多輪對話上下文理解

**長期記憶（Persistent Memory）**
- 使用 PostgreSQL 資料庫
- 永久保存舌診分析記錄
- 用戶 ID 儲存在 localStorage（跨會話保留）
- 自動觸發保存機制

#### 自動觸發時機

```python
# 1. 舌診分析完成後自動保存
memory_manager.long_term.save_tongue_analysis(
    user_id=user_id,
    session_id=session_id,
    prediction_results=prediction_results,
    analysis_response=response_text,
    additional_info=additional_info
)

# 2. 保存內容包括：
- 預測結果（JSON 格式）
- AI 分析回應（完整文本）
- 用戶 ID 與會話 ID
- 建立時間戳記
```

#### 資料庫結構

```sql
-- 用戶記憶表
CREATE TABLE user_memory (
    user_id VARCHAR(255) PRIMARY KEY,
    preferences TEXT,  -- JSON 格式的用戶偏好
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 舌診分析記錄表
CREATE TABLE tongue_analysis_records (
    id SERIAL PRIMARY KEY,
    user_id VARCHAR(255),
    session_id VARCHAR(255),
    prediction_results TEXT,  -- JSON 格式的預測結果
    analysis_response TEXT,   -- LLM 分析的回應
    additional_info TEXT,     -- 額外信息
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES user_memory(user_id)
);
```

### 3. 健康雷達圖

#### 六個評估維度

基於中醫體質分型理論，評估以下六個維度：

```
        氣虛
         │
   血瘀 ─┼─ 血虛
         │
    濕熱─┼─ 陰虛
         │
        陽虛
```

#### 評分計算邏輯

**步驟 1：權重映射**

每個症狀對應不同維度的權重分數（1-4分）：

```python
SYMPTOM_WEIGHTS = {
    "TonguePale": {"氣虛": 2, "血虛": 2, "陽虛": 1},  # 舌淡白
    "Toothmark": {"氣虛": 3, "陽虛": 2},            # 齒痕
    "Ecchymosis": {"血瘀": 4},                      # 瘀斑 (最嚴重)
    "FurYellow": {"濕熱": 3},                       # 苔黃
    # ...
}
```

**步驟 2：累加計算**

```python
# 範例：檢測到 [舌淡白, 齒痕, 苔厚]
scores = {
    "氣虛": 2 + 3 + 1 = 6 分,
    "血虛": 2 分,
    "陽虛": 1 + 2 = 3 分,
    "濕熱": 2 分,
    "陰虛": 0 分,
    "血瘀": 0 分
}

# 限制最高分為 10 分
scores[dim] = min(10.0, scores[dim])
```

**步驟 3：視覺化**

- **單日檢視**：顯示選定日期的雷達圖
- **一週平均**：計算最近 7 天的平均值
- **趨勢對比**：左右切換查看不同日期的變化

#### 雷達圖範例

```
       氣虛 (6)
          /\
         /  \
血瘀(0) /    \ 血虛(2)
       /  ★  \
      /  用戶  \
濕熱(2)\      / 陰虛(0)
        \    /
         \  /
          \/
        陽虛(3)
```

### 4. AI 健康建議

基於過去一週的舌診記錄，自動生成個性化建議：

```python
# API 端點：POST /api/tongue/advice
{
    "user_id": "user_123"
}

# 返回範例：
{
    "advice": """
    根據您過去一週的舌診分析，您有以下健康狀況：

    1. 氣虛症狀較為明顯（出現5次）
       - 建議：多食用黃耆、黨參等補氣食材
       - 避免過度勞累，保證充足睡眠

    2. 濕熱體質有輕微傾向（出現3次）
       - 建議：減少油膩、辛辣食物
       - 可飲用薏仁水、綠豆湯清熱利濕

    3. 生活建議：
       - 規律作息，早睡早起
       - 適度運動，如太極拳、八段錦
       - 保持心情舒暢
    """
}
```

---

## 💻 技術棧

### 前端技術

| 技術 | 版本 | 用途 |
|-----|------|-----|
| **Electron** | 39.2.2 | 跨平台桌面應用框架 |
| **React** | 19.2.0 | UI 組件化開發 |
| **TypeScript** | 最新 | 型別安全 |
| **Vite** | 7.2.2 | 快速構建工具 |
| **Tailwind CSS** | 最新 | 樣式設計 |
| **Recharts** | 最新 | 雷達圖視覺化 |
| **Lucide React** | 最新 | 圖標庫 |

### 後端技術

| 技術 | 版本 | 用途 |
|-----|------|-----|
| **Python** | 3.11+ | 後端語言 |
| **FastAPI** | 最新 | Web 框架 |
| **LangChain** | 最新 | LLM 應用框架 |
| **LangGraph** | 最新 | Agent 工作流編排 |
| **Ollama** | 最新 | 本地 LLM 運行環境 |
| **Qwen3:8b** | 8B 參數 | 開源中文大模型 |
| **PostgreSQL** | 最新 | 關聯式資料庫 |
| **SQLAlchemy** | 最新 | ORM 框架 |

### AI 模型

| 模型 | 架構 | 用途 |
|-----|------|-----|
| **Swim Transformer** | ViT-based | 舌頭分割（384×384） |
| **ConvNeXt Base** | CNN-based | 多標籤分類（8 類） |
| **Qwen3:8b** | Transformer | 中醫理論分析 |

### 開發工具

- **套件管理**：pnpm (前端) / uv (後端)
- **版本控制**：Git
- **API 測試**：Postman / curl
- **容器化**：Docker Compose

---

## 📊 雷達圖評分系統深度解析

### 為什麼選擇這 6 個維度？

基於中醫**體質辨識**理論，這 6 個維度涵蓋了最常見的亞健康體質類型：

| 維度 | 臨床意義 | 常見症狀 |
|-----|---------|---------|
| **氣虛** | 臟腑功能減退 | 疲勞、氣短、易出汗 |
| **血虛** | 血液營養不足 | 面色蒼白、頭暈、心悸 |
| **陰虛** | 體液不足、虛熱 | 口乾、盜汗、失眠 |
| **陽虛** | 機能減退、畏寒 | 怕冷、手腳冰涼、腰酸 |
| **濕熱** | 濕邪與熱邪並存 | 口苦、大便黏膩、皮膚油膩 |
| **血瘀** | 血液循環不暢 | 面色暗沉、疼痛、瘀斑 |

### 權重設計原則

**1. 症狀嚴重度**
- 輕度特徵：1 分（如 苔厚 → 濕熱）
- 中度特徵：2 分（如 舌淡白 → 氣虛）
- 重度特徵：3-4 分（如 瘀斑 → 血瘀）

**2. 臨床相關性**
- 一個症狀可能影響多個維度
- 範例：`舌淡白` 同時影響「氣虛」、「血虛」、「陽虛」

**3. 中醫理論依據**

```python
# 範例：齒痕 (Toothmark)
"Toothmark": {"氣虛": 3, "陽虛": 2}

# 理論依據：
# - 齒痕主要反映脾氣虛 (權重 3)
# - 也可能伴隨陽虛 (權重 2)
```

### 評分等級劃分

| 分數範圍 | 等級 | 建議 |
|---------|------|-----|
| 0-2 分 | 🟢 正常 | 維持現狀 |
| 3-4 分 | 🟡 輕度 | 注意飲食與作息 |
| 5-6 分 | 🟠 中度 | 建議調理 |
| 7-10 分 | 🔴 重度 | 建議就醫 |

### 數據來源與查詢

**API 端點**：
```
GET /api/reports/weekly?user_id={userId}
```

**返回格式**：
```json
[
  {
    "date": "2025-12-20",
    "scores": {
      "氣虛": 6,
      "血虛": 2,
      "陰虛": 0,
      "陽虛": 3,
      "濕熱": 2,
      "血瘀": 0
    }
  },
  {
    "date": "2025-12-21",
    "scores": {
      "氣虛": 5,
      "血虛": 2,
      "陰虛": 1,
      "陽虛": 2,
      "濕熱": 3,
      "血瘀": 0
    }
  }
]
```

---

## 🔄 使用流程

### 用戶端操作流程

```
1. 啟動應用
   ├─ 自動生成/讀取 user_id (存於 localStorage)
   └─ 生成新的 session_id

2. 拍攝舌頭照片
   ├─ 使用相機模組即時拍攝
   └─ 或上傳本地圖片

3. 發送分析請求
   ├─ 前端將圖片轉為 Base64
   └─ 調用 POST /tongue/predict-and-analyze/stream

4. 後端處理流程
   ├─ Vision-Predict 模型分析
   │   ├─ 分割舌頭
   │   ├─ 提取 ROI
   │   └─ 分類 8 種特徵
   │
   ├─ LangGraph Agent 生成分析
   │   ├─ 調用 Qwen3:8b LLM
   │   └─ 流式返回中醫解釋
   │
   └─ 自動保存到 PostgreSQL
       ├─ user_id
       ├─ prediction_results
       ├─ analysis_response
       └─ created_at

5. 前端顯示結果
   ├─ 流式顯示 AI 分析
   └─ 狀態提示（分析中、完成）

6. 查看健康週報
   ├─ 點擊「健康週報」按鈕
   ├─ 獲取過去 30 天記錄
   ├─ 生成雷達圖
   └─ 生成 AI 建議
```

### 技術實現流程

**前端 (App.tsx)**
```typescript
// 1. 用戶 ID 初始化
const [userId] = useState(() => {
  let storedUserId = localStorage.getItem("tongue_ai_user_id");
  if (!storedUserId) {
    storedUserId = `user_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;
    localStorage.setItem("tongue_ai_user_id", storedUserId);
  }
  return storedUserId;
});

// 2. 發送圖片分析請求
await predictAndAnalyzeTongueImage(
  {
    imageFile: imageUrl,
    additional_info: userMessage,
    user_id: userId,
    session_id: sessionId,
  },
  onChunk,    // 流式接收 AI 回應
  onStatus,   // 狀態更新
  onComplete, // 完成回調
  onError     // 錯誤處理
);
```

**後端 (routes.py)**
```python
# 3. 接收並處理請求
@tongue_router.post("/predict-and-analyze/stream")
async def predict_and_analyze_tongue_stream(
    file: UploadFile = File(...),
    user_id: Optional[str] = Form("default"),
    session_id: Optional[str] = Form(None)
):
    # 3.1 調用 Vision-Predict
    prediction_results = vision_loader.analyze(image_path)
    
    # 3.2 調用 LangGraph Agent (流式)
    async for event in tongue_agent.astream_events(initial_state, config):
        if event["event"] == "on_chat_model_stream":
            yield chunk  # 流式返回
    
    # 3.3 自動保存到資料庫
    memory_manager.long_term.save_tongue_analysis(
        user_id=user_id,
        prediction_results=prediction_results,
        analysis_response=full_response
    )
```

**資料庫查詢 (main.py)**
```python
# 4. 健康週報 API
@app.get("/api/reports/weekly")
def get_weekly_chart_data(user_id: str):
    # 4.1 查詢過去 30 天記錄
    records = memory_manager.long_term.get_tongue_analysis_history(
        user_id=user_id,
        limit=50,
        start_date=datetime.now() - timedelta(days=30)
    )
    
    # 4.2 計算雷達圖分數
    report_data = TongueHealthScorer.generate_weekly_report_data(records)
    
    return report_data["chart_data"]
```

---

## ✨ 特色亮點

### 1. 本地部署，隱私優先

- ✅ 所有數據存儲在本地 PostgreSQL
- ✅ LLM 使用 Ollama 本地運行
- ✅ 不依賴雲端服務，保護用戶隱私

### 2. 智能 Agent 架構

採用 LangGraph 實現三種 Agent 模式：

```python
# Chat Agent - 一般對話
agents["chat"] = create_unified_agent(AgentMode.CHAT)

# Tongue Agent - 舌診分析
agents["tongue"] = create_unified_agent(AgentMode.TONGUE_ANALYSIS)

# Tool Agent - 帶工具調用
agents["tool"] = create_unified_agent(AgentMode.TOOL_ENABLED)
```

### 3. 流式響應，即時反饋

- 使用 Server-Sent Events (SSE)
- 逐字顯示 AI 分析結果
- 提升用戶體驗

### 4. 長期記憶自動化

- 無需手動保存
- 每次舌診自動記錄
- 支持跨會話查詢

### 5. 多平台支持

```bash
# macOS
pnpm run dist:mac

# Windows
pnpm run dist:win

# Linux
pnpm run dist:linux
```

### 6. 模組化設計

```
fastapi-service/
├── agents/        # Agent 邏輯
├── config/        # 配置與評分
├── utils/         # 工具模組
│   ├── memory_manager.py    # 記憶管理
│   ├── vision_loader.py     # 視覺模型加載
│   └── streaming.py         # 流式處理
└── routes.py      # API 路由
```

---

## 📈 系統性能

### 模型推理速度

| 模型 | 輸入大小 | 推理時間 | 硬體需求 |
|-----|---------|---------|---------|
| Swim Transformer | 384×384 | ~200ms | GPU 推薦 |
| ConvNeXt Base | 224×224 | ~150ms | GPU 推薦 |
| Qwen3:8b | Token-based | ~50 tokens/s | 16GB RAM |

### API 響應時間

| 端點 | 平均響應時間 | 說明 |
|-----|------------|-----|
| `/chat/stream` | 即時流式 | 逐字返回 |
| `/tongue/predict` | 1-2 秒 | 圖片預測 |
| `/tongue/predict-and-analyze/stream` | 3-5 秒 | 完整流程 |
| `/api/reports/weekly` | <500ms | 資料庫查詢 |

### 資源佔用

- **記憶體**：~4GB (包含 Ollama)
- **硬碟**：~10GB (模型 + 資料庫)
- **網路**：僅需啟動時下載模型

---

## 🎯 應用場景

### 1. 個人健康管理

- 每日舌診自我檢測
- 追蹤體質變化趨勢
- 及早發現健康問題

### 2. 中醫診所輔助

- 標準化舌診記錄
- 量化評估工具
- 長期療效追蹤

### 3. 中醫教學

- 學生實習訓練
- 舌診特徵識別
- 理論與實踐結合

### 4. 健康諮詢服務

- 線上健康評估
- 個性化調理建議
- 遠程醫療支持

---

## 🚧 未來發展

### 短期計劃（1-3 個月）

- [ ] **增加更多舌診特徵**
  - 舌體胖瘦
  - 舌質紅絳
  - 舌苔膩度

- [ ] **優化模型性能**
  - 提高分割準確度
  - 減少推理時間
  - 支持 CPU 運行

- [ ] **增強 AI 建議**
  - 飲食建議庫
  - 穴位按摩指導
  - 運動處方

### 中期計劃（3-6 個月）

- [ ] **多模態輸入**
  - 問診問卷
  - 脈診數據整合
  - 面診圖片分析

- [ ] **社群功能**
  - 用戶健康日記
  - 經驗分享
  - 專家問答

- [ ] **移動端應用**
  - iOS / Android 原生 App
  - 雲端同步

### 長期計劃（6-12 個月）

- [ ] **臨床研究合作**
  - 與醫院合作驗證
  - 發表學術論文
  - 取得醫療器材認證

- [ ] **知識圖譜構建**
  - 中醫症狀關聯
  - 方劑推薦系統
  - 個性化治療方案

- [ ] **商業化**
  - SaaS 版本
  - API 服務
  - 企業版授權

---

## 📚 技術挑戰與解決方案

### 挑戰 1：模型準確度

**問題**：舌診特徵細微，光線、角度影響大

**解決方案**：
- 數據增強（旋轉、翻轉、色彩調整）
- 多模型集成
- 引入不確定性量化

### 挑戰 2：LLM 幻覺問題

**問題**：LLM 可能生成不準確的中醫建議

**解決方案**：
- RAG（檢索增強生成）引入權威知識庫
- 提示工程（Prompt Engineering）限制回答範圍
- 人工審核高風險建議

### 挑戰 3：記憶管理效率

**問題**：長期記憶查詢可能變慢

**解決方案**：
- PostgreSQL 索引優化
- 分頁查詢
- Redis 緩存熱門數據

### 挑戰 4：跨平台部署

**問題**：不同系統環境差異大

**解決方案**：
- Docker 容器化
- Electron 統一打包
- CI/CD 自動化測試

---

## 🎓 學習資源

### 中醫理論

- 《中醫診斷學》
- 《舌診圖譜》
- 《體質辨識理論》

### 技術文檔

- [LangChain 官方文檔](https://python.langchain.com/)
- [LangGraph 教程](https://langchain-ai.github.io/langgraph/)
- [FastAPI 文檔](https://fastapi.tiangolo.com/)
- [Electron 文檔](https://www.electronjs.org/)

### 相關論文

- Swim Transformer: Hierarchical Vision Transformer
- ConvNeXt: A ConvNet for the 2020s
- 中醫舌診圖像分割與識別研究進展

---

## 👥 團隊與貢獻

### 專案維護者

- **開發者**：[您的名字]
- **聯絡方式**：[郵箱]
- **GitHub**：[倉庫連結]

### 貢獻指南

歡迎提交 Issue 和 Pull Request！

```bash
# Fork 倉庫
git clone https://github.com/your-repo/tongue-ai-agent.git

# 創建分支
git checkout -b feature/your-feature

# 提交更改
git commit -m "Add your feature"

# 推送並創建 PR
git push origin feature/your-feature
```

---

## 📄 授權協議

本專案採用 **MIT License**

---

## 🎉 致謝

感謝以下開源專案：

- LangChain & LangGraph
- Ollama
- Qwen Team
- PyTorch
- FastAPI
- Electron
- React

---

## 📞 聯絡方式

- **Email**：your-email@example.com
- **GitHub Issues**：[提交問題](https://github.com/your-repo/issues)
- **文檔**：[線上文檔](https://your-docs-url.com)

---

## 🌟 Star History

如果這個專案對您有幫助，請給我們一個 ⭐️！

---

**最後更新**：2025 年 12 月

**版本**：v1.0.0

**狀態**：✅ 開發中

---

# Thank You! 🙏

**讓中醫智慧與 AI 技術相結合，守護每個人的健康！**


